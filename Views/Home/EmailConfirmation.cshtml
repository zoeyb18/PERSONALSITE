@{
    ViewData["Title"] = "Email Confirmation";
}

@model pers1102.Models.ContactViewModel

<section class="contact" data-aos="fade-up" data-aos-easing="ease-in-out" data-aos-duration="500">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="info-box">
                    <i class="bx bx-check-circle"></i>
                    <h3>Message Sent!</h3>
                    <p>
                        Thanks for contacting me! I will
                        respond to your message using the provided email address,
                        @Model.Email.
                    </p>

                    <br />

                    <div class="offset-3 col-md-6 text-center">
                        <p>
                            You sent the following message on
                            @DateTime.Now.Date.ToShortDateString() at
                            @DateTime.Now.ToShortDateString()
                        </p>
                    </div>

                    <br />

                    <div class="offset-3 col-md-6">
                        <p>
                            <em>@Model.Message</em>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>